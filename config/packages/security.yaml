security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'
    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\LoginFormAuthenticator
            entry_point: App\Security\LoginFormAuthenticator
            logout:
                path: app_logout
                target: app_login

            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
                path: /
                # by default, the feature is enabled by checking a checkbox in the
                # login form, uncomment the following line to always enable it.
                #always_remember_me: true

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    role_hierarchy:
        ROLE_ASSISTANT: [ROLE_USER]
        ROLE_AVOCAT:    [ROLE_ASSISTANT]
        ROLE_ADMIN:     [ROLE_AVOCAT]

    access_control:
        # Routes publiques
        - { path: ^/(login|register|logout), roles: PUBLIC_ACCESS }
        # Administration et gestion des utilisateurs : ADMIN uniquement
        - { path: ^/admin,    roles: ROLE_ADMIN }
        - { path: ^/users,    roles: ROLE_ADMIN }
        # Dossiers et factures : AVOCAT + ADMIN (via hiérarchie)
        - { path: ^/dossiers, roles: ROLE_AVOCAT }
        - { path: ^/factures, roles: ROLE_AVOCAT }
        # Documents, clients, tâches, temps : tous les rôles connectés
        - { path: ^/document, roles: ROLE_ASSISTANT }
        - { path: ^/client,   roles: ROLE_ASSISTANT }
        - { path: ^/tache,    roles: ROLE_ASSISTANT }
        - { path: ^/entree,   roles: ROLE_ASSISTANT }
        - { path: ^/rendez-vous, roles: ROLE_ASSISTANT }
        - { path: ^/profile,  roles: ROLE_ASSISTANT }
        # Tout le reste : utilisateur connecté minimum
        - { path: ^/,         roles: ROLE_ASSISTANT }

when@test:
    security:
        password_hashers:
            # By default, password hashers are resource intensive and take time. This is
            # important to generate secure password hashes. In tests however, secure hashes
            # are not important, waste resources and increase test times. The following
            # reduces the work factor to the lowest possible values.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
